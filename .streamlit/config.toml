# Streamlit theme — matched to polarisstudent.com's brand system.
# (Colors mirror the site's CSS variables; see THEME.md for the full token list.)
[theme]
primaryColor = "#b22222"            # --brand (firebrick)
backgroundColor = "#ffffff"         # --white
secondaryBackgroundColor = "#f8fafc" # --bg-alt
textColor = "#1e293b"               # --text
font = "sans serif"
